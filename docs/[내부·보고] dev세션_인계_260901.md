# [내부·보고] dev 세션 인계 (260901 야간 마감 — 1차 구현 지시서 1호)

## 결론

- 지시서 §순서 1~8 **전부 구현·커밋** — 브랜치 `feat/phase1-260901` (8커밋, origin 푸시 완료) · main 미변경 · 안정 지점 태그 `stable-260901` = `98e76c3`
- 프리뷰 배포 = https://employee-directory-git-feat-phase1-260901-zzo1.vercel.app (Vercel **Deployment Protection 켜져 있음** — Vercel 로그인한 계정만 열림. 독립 검증 세션에 열려면 대시보드 → Settings → Deployment Protection 에서 Preview 보호 해제 또는 Share 링크 발급)
- 스키마 변경 **5개 파일**(지시서 4건 + 검증 중 발견한 결함 수정 1건) — 전부 **원격 Supabase(ilukscvucabsxbfmipbt)에 적용 완료**. 전부 추가형(컬럼·표·함수)이라 현 프로덕션(c650e10) 화면에 영향 없음 — 9/2 시연 전 프로덕션 재확인만 권장
- 다음: 독립 검증 세션(만든 자 ≠ 검증자) → 이슈 전량 기록 → 지시서 2호. 9/2 시연 후 기획 확인 뒤 main 머지

## 반영 (260901, 8단계 8커밋)

| 단계 | 커밋 | 내용 |
|---|---|---|
| 1 A1 | 4149027 | 인사 이슈 보드(8유형 · 유형 칩 · D-Day · 상태 점 · 행 클릭 → 인사카드) + KPI 5칸(이슈 lead 주황 · 현원 · 재직 · 휴직 · 퇴사예정), 퇴사 누적·평균 근속 → 인원 변동 unit · 마이그레이션 ① |
| 2 A2·A3 | 2577031 | 부서별 연차 사용(합계행 = 구 연차 카드 · 소진율 하위 2 표시) · 이달 입퇴사(월 선택 · 엑셀) · A-3 배치 · 부서별 이직·거주지 접힘 기본(저장 키 v3) |
| 3 B1 | cf710c7 | interviews 신설(마이그레이션 ②) · 인사카드 **성장 탭** 신설 · 면담 기록 추가/수정/삭제 |
| 4 B2 | e123745 | recognitions 신설(마이그레이션 ③) · 인정 이력 CRUD · 성장 요약 3칸(인정 건수 · 최근 면담 · 다음 면담) — 인정(축) → 면담(부가층) 순 |
| 5 B5·B4 | f059ba9 | 비상연락망 값/미기재 배지(기본 탭 · 헤더 칩) · 폼에 비상연락망·계약종료일·복직예정일 · 중복 검증(사번 차단 · 메일/휴대전화 경고 + 확인 체크) |
| 6 B6·B3 | 48df338 | 엑셀 다운로드 컬럼 선택(기본/개인정보 2그룹) · photo_url + 스토리지 버킷(마이그레이션 ④) · 생성 더미 아바타 · 업로드(320px 축소) · 대장 썸네일 |
| 7 B7 | f8463fc | 조직도 탭(계열사 › 부서 트리 · 현원 · 접기 · 부서 클릭 → 대장 · 명칭 수정) · **rename_department RPC**(마이그레이션 ⑤) |
| 8 A4 | ea102cb | 면담 진행률(완료 % · 유형별 · 부서별 이행률 가나다순 · 90일 무기록 인원 목록) · [3] grid2 면담 진행률 | 부서별 연차 |

- 검증 캡처: `app/docs/verify/260901/` — 항목번호_설명.png 60여 장 (빈 값 · 검증 오류 · 접힘/펼침 · 추가/수정/삭제 전후 · 회귀 REG_*). 로컬 dev(:3111) 실화면, Playwright 캡처
- 회귀 확인: 대장 필터·정렬 · 인사카드 7탭 순회 · R6 퇴사일 필수 · 변경이력 페이지 · 연차 조회 · 콘솔 에러 0 (REG_* 캡처)

## 스키마 변경 (원격 적용 완료 · `supabase migration list` 일치)

| # | 파일 | 내용 | 지시서 |
|---|---|---|---|
| ① | `20260901010000_issue_board_columns.sql` | employees.**contract_end_date** · **return_date** · **emergency_contact** 추가 + 결정적 더미 + log_change 감시 필드 | §B-4 "복직예정일 부재 시 추가" — **계약종료일·비상연락망도 부재**라 함께 추가(가정 1) |
| ② | `20260901020000_interviews.sql` | interviews(유형 6종 · created_by 트리거 · RLS) + 더미 121건 | B1 |
| ③ | `20260901030000_recognitions.sql` | recognitions(유형 5종 · registered_by 트리거 · RLS) + 더미 71건 | B2 |
| ④ | `20260901040000_photo_url.sql` | employees.photo_url + 생성 더미(SVG data URI) + storage 버킷 `employee-photos`(공개 읽기 · 쓰기 로그인) + log_change('(사진 변경)'만) | B3 |
| ⑤ | `20260901050000_rename_department.sql` | **rename_department(code, name)** RPC(authenticated 전용) + log_appointment 가드 + 검증 잔재 정리 | 지시서 밖 — **결함 수정**(아래) |

- ⑤ 사유: departments.name 을 직접 update 하면 employees FK cascade 가 발령 트리거를 깨워 소속 전원에게 "A → B 부서 이동" **발령이 자동 생성**됨(검증 중 9명 × 2회 = 18건 실제 발생 → 마이그레이션에서 삭제). 명칭 변경 ≠ 발령이므로 RPC 안에서 트랜잭션 로컬 플래그로 부서 이동 발령만 건너뜀. change_log 는 그대로 남김
- 더미 데이터 상태: 158명 유지 · 상태 분포 불변. 검증 중 GA25096(박지호) 사진만 테스트 패턴 이미지로 교체됨(스토리지 `GA25096.jpg`) — 원복하려면 photo_url 을 null 로 두고 마이그레이션 ④의 더미 식으로 재생성

## 가정 (지시서에 값이 없어 단순한 쪽 선택 — 인터뷰·기획 확정 시 교체)

| # | 항목 | 가정값 | 위치 |
|---|---|---|---|
| 1 | 이슈 보드 입력 컬럼 | 계약종료일·비상연락망도 employees 에 추가 (지시서는 "신규 컬럼 없음"으로 적었으나 부재) | 마이그레이션 ① |
| 2 | 수습 기간 | 입사 후 **3개월**, 회장 제외 | `lib/issues.ts` |
| 3 | 입사 100일 | 입사일 + 100일 | 동일 |
| 4 | 근속 도래 | 7·10·20년 기념일이 **올해**인 사람 상시 표시(지난 것 = '도래', 남은 것 = '예정') — 260901 확정 반영 | 동일 |
| 5 | 이슈 상태 척도 | 경과(D+) · 임박(D-3) · 주의(D-15) · 예정 — 계약 만료 기준을 모든 기한 유형에 동일 적용 | 동일 |
| 6 | 보드 창 | 향후 30일 · 기준일 지난 계약·복직은 '경과'로 남김(조용히 빠지면 놓침) | 동일 |
| 7 | 계약직 더미 계약종료일 | 입사 기념일 1년 갱신 가정 → 기준일 이후 첫 기념일 · 인턴 = 입사 + 1년 | 마이그레이션 ① |
| 8 | 엑셀 형식 | **UTF-8 BOM CSV**(엑셀 더블클릭 열림) — xlsx 라이브러리 미도입 | `lib/csv.ts` |
| 9 | 엑셀 컬럼 기본값 | 대장 표 10열 켜짐 · 개인정보 5열(생년월일·메일·휴대전화·비상연락망·거주지역) 꺼짐 | `ExportDialog.tsx` |
| 10 | 중복 검증 범위 | 사번(신규, 차단) · 메일계정 · 휴대전화(숫자만 비교) · 본인 번호 = 비상연락망. **주민번호는 보류**(스키마 없음, R9) | `EmployeeForm.tsx` |
| 11 | 사진 | 브라우저 320px 정사각 중앙 크롭 JPEG · 원본 미보관 · 파일명 = 사번 | `lib/photo.ts` |
| 12 | 조직도 계층 | **계열사 › 부서 2단** — '사업부' 층은 departments 컬럼 부재. 계열사 명칭 수정 제외(두 표 자유 텍스트) | `OrgChart.tsx` |
| 13 | 면담 진행률 대상 | 올해 도래한 100일·1년·수습 + 입사 1년 이상 현원의 **반기 정기 1회** · 완료 = 같은 유형 기록(이벤트 ±60일) · 공백 = **90일**(로드맵 제안값) | `lib/interviewStats.ts` |
| 14 | 성장 탭 순서 | 요약(인정 · 최근/다음 면담) → 인정 이력 → 면담 기록 — 설계 260901 "인정 = 축, 면담 = 부가층" | `GrowthTab.tsx` |
| 15 | 면담·인정 더미 | 사번 규칙(n%5=0 무기록 · n%3=1 인정 1건 · n%7=2 추가) — 정기 면담이 상반기에 몰려 하반기 이행률 22%, 90일 무기록 97명으로 보임. 시연용으로 낮게 보이면 시드 조정 | 마이그레이션 ②③ |
| 16 | 인사카드 탭 순서 | 기본 · 조직 · 고용·계약 · 급여 · 경력·자격 · **성장** · 법정 | `EmployeeDetail.tsx` |
| 17 | 접힘 리셋 | CollapsibleCard 저장 키 v2 → v3 (부서별 이직·거주지 접힘 기본을 실제 적용) | `CollapsibleCard.tsx` |

## 보류 / 미완

| 항목 | 상태 | 비고 |
|---|---|---|
| 주민번호 중복 검증 (B4 일부) | 보류 | 저장 컬럼(해시)이 없어 비교 불가 — `rrn_hash` 컬럼 도입은 S1 암호화·권한 설계와 함께 결정 |
| 조직도 사업부 층 · 계열사 명칭 수정 (B7 일부) | 보류 | departments 에 계층 컬럼(division) 추가 후 — 트리는 한 층 끼우면 됨 |
| 프리뷰 URL 외부 열람 | 미확인 | Vercel Deployment Protection(SSO) — 검증 세션 전 보호 해제 또는 Share 링크 필요 |
| role 세분화 · Disable signup | 보류 유지 | 260825 인계 그대로 |
| 배포 URL 독립 검증 | 미실시 | 이 세션은 로컬 실화면 캡처까지. 완료 기준의 "배포 URL 검증"은 검증 세션 몫 |
| lint 기존 오류 3건 | 처리 | `react-hooks/set-state-in-effect`(page · CollapsibleCard · EmployeeForm) — 동작 변경 없이 사유 주석 + 라인 억제. 커밋 조건(lint 통과) 충족용 |

## 회귀 위험 판단

- **log_appointment 교체**(마이그레이션 ⑤): 본문은 260823 정본과 동일 + `app.dept_rename` 가드 1줄. R8 발령 자동기록은 REG_card_org_tab_appointments 로 확인했으나 발령 트리거는 프로덕션 공용 — 9/2 시연 전 프로덕션에서 발령 1건 생성 재확인 권장
- **log_change 교체** 2회(①④): 감시 필드 배열만 확장 · photo_url 은 '(사진 변경)'만 기록
- **KPI·위젯 재배치**: 기존 카드 삭제는 '연차 사용 현황' 1건(A2 합계행으로 흡수). 인원 변동·부서별 이직·거주지·고용형태 코드는 그대로
- **EmployeeForm 필수 prop 추가**(`rows`) — 호출부 page.tsx 1곳만
- 프리뷰가 현 프로덕션과 같은 Supabase 를 쓴다 — 검증 세션에서 등록·수정·삭제한 값은 프로덕션 화면에도 보인다. 검증은 생성한 것을 되돌리는 원칙으로

## 검증 캡처 위치

- `app/docs/verify/260901/` (git 추적 — `.gitignore` 에 `!docs/verify/**` 예외 추가)
- 명명: `A1_*`(보드 · 필터 · 빈 유형 · KPI · 행 클릭) · `A2_*` · `A3_*`(빈 월 · 입사 월 · 퇴사 월) · `A-2_*`(재배치 · 접힘/펼침) · `A-3_layout_fullpage` · `A4_*` · `B1_*`(빈 상태 · 검증 오류 · 추가/수정/삭제 전후) · `B2_*` · `B3_*`(더미 · 업로드 후 · 썸네일) · `B4_*`(경고 · 차단 · 확인) · `B5_*`(배지 · 칩 · 폼) · `B6_*`(기본 · 선택 · 0열 비활성) · `B7_*`(트리 · 접힘 · 수정 중 · 변경 후 · 대장 반영) · `REG_*`(회귀)

## 다음 세션 입력

1. **독립 검증 세션** — 정본: 이 파일 + `[내부·기획] 인사시스템_1차구현_dev지시서_260901.md`. 프리뷰 보호 해제 후 A1~A4 · B1~B7 항목별 확대 캡처 재검증 · 이슈 전량 기록(GitHub 이슈 권장)
2. **기획 확인 대상**(가정 표 #1 · #2 · #5 · #13 · #15) — 특히 이슈 보드 상태 척도와 면담 진행률 대상 정의는 인사팀 확정값으로 교체
3. **머지**: 9/2 시연 종료 → 기획 확인 → `feat/phase1-260901` → main (머지 후 Vercel 프로덕션 자동 배포). 되돌릴 일이 생기면 `stable-260901`
4. 항목사전 v0.2 반영 항목: contract_end_date · return_date · emergency_contact · photo_url · interviews · recognitions

## 전제 (뒤집으면 안 되는 확정)

- 디자인 토큰 값 불변 (`design-tokens.css` 무변경) · 표 셀 13px · 강조 주황 = KPI '이슈' 1곳(카드 합계행은 잉크색으로 내림) · 그림자 없음
- 부서 명칭 변경은 **반드시 rename_department RPC** — 직접 update 하면 발령 18건 결함 재발
- `supabase db reset` 실행 시 직후 `bash scripts/seed_demo_user.sh` (auth 계정 소실)
- 실데이터 반입 금지 — 더미 158명 · 사진은 생성 더미(테스트 업로드 1건 제외)

## 잔재 정리 (260901 야간 — 독립 검증 이슈 #1·#2 · 사용자 승인)

- 대상: 공용 Supabase 에 남은 9/1 dev 검증 잔재. 9/2 시연이 프로덕션(같은 DB)을 쓰므로 시연 전 삭제 — 위 "change_log 는 그대로 남김"은 이 절로 대체
- 방법: `supabase db query --linked` 트랜잭션 1건 (코드 변경 · 커밋 없음)

| 대상 | 특정 조건 | 건수 |
|---|---|---|
| change_log — 총무팀 9명 부서명 왕복 | 9/1 · field=department · (총무팀 ↔ 총무·시설팀) | 18 |
| change_log — 임소율 GA20089 | 9/1 · effective_date 1 + emergency_contact 추가/삭제 2 | 3 |
| change_log — 박지호 GA25096 사진 변경 | 9/1 · photo_url | 1 |
| change_log — 사진 원복이 만든 행 | 같은 트랜잭션 안 · 위 photo_url 조건에 포함 | 1 |
| employees.photo_url GA25096 | 테스트 패턴 URL → 마이그레이션 ④ 더미 식(SVG data URI)으로 재생성 | 1 |
| storage `employee-photos/GA25096.jpg` | Storage API DELETE(로그인 계정) | 1 |

| 검증 | 전 | 후 |
|---|---|---|
| change_log 전체 | 25 | **3** (9/1 이후 0건 — 잔재 삭제 전 정상 기준) |
| employees | 158 | 158 |
| appointments | 250 | 250 |
| departments | 10 | 10 |
| storage.objects(employee-photos) | 1 | 0 |

- 삭제 보류 행: 없음 (9/1 생성 행 22건 전부 조건 일치)

## 긴급 소건 (260902 오전 — 2호 후보 C2·C3 선반영)

- C2 `3cdffbc` — '평가' 탭 신설: 경력·자격의 "성과 이력 (평가)" 섹션 이동 + 재무목표 달성률·MBO 과제·연간 평가 점수 자리(수집 예정 배지 · 등급 칸 없음) · C3 커밋 — '근태' 탭 신설: 고용·계약의 연차(발생·사용·잔여+산정 콜아웃) 이동 + 근무시간·초과근무 자리. 둘 다 UI만, 스키마 없음
- 최종 탭 순서: 기본 · 조직 · 고용·계약 · 급여 · **근태** · 경력·자격 · 성장 · **평가** · 법정 — 캡처 `docs/verify/260901/C2_평가탭.png` · `C3_근태탭.png`
