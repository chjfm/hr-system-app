# [내부·보고] dev 세션 인계 (260825 야간 마감)

## 결론

- 최종 배포 = **c650e10** (Vercel Production Ready · https://employee-directory-sigma-henna.vercel.app)
- 8/26 14:00 인사팀 미팅은 이 배포로 진행 — 탭 6종 인사카드 · 현황/직원 대장/변경 이력 · 고용형태 · 연차 조회 포함
- 보류 2건 + 인터뷰 대기 3건 → 아래 표. 착수 전 이 파일과 `수집 예정` 배지 자리(스키마 없음)를 전제로 확인

## 오늘 반영 (260825, 배포 10회)

| 배치 | 커밋 | 내용 |
|---|---|---|
| 인사카드 | 1aa77c6 | 직원 상세 → 탭 6개(항목사전 v0.1.0 순서) · 수집 예정 배지 · 주민번호/계좌 마스킹 데모 |
| 디자인 정돈 | 719cd30 | 홈 위계 재배치 · 상세 모달 → 우측 480px 슬라이드 패널 · 간격 4/8/16 |
| 화면 분리 | d75bcfa | 상단 탭 현황/직원 대장(랜딩=현황) · 칩/배지 무채색 규율 |
| 검증 이슈 | f7184f0 | 접힘 저장 키 v2 리셋 · 성과 표 위계/스크롤 · 퇴사일 요약줄 병기 |
| 2차 조형 | 004c01b·9b801d0·1b33f36 | P1 타이틀 · P2 KPI 개별 카드 · P3 여백 리듬 · P5 데이터 13px/헤더 배경 · P4 폭 정렬 |
| 차트 감리 | 148e5ec | 인원 변동 2색 채움(주황/회색)·라벨 제거 · 버블맵 상위 5 숫자·흰 링 |
| 토글·P6~P8 | dfd9838·33a1c73·d369808 | 헤더 행 토글+SVG 셰브론 · 패널 값 12px · 표 밀도 · 이력 탭 → 경력·자격, 발령 → 조직 탭, 성과 = "성과 이력 (평가)" |
| 야간 기능 | 63c77df·68f0d30·c650e10 | self-signup 제거+create_user.sh · **고용형태**(5종+분포+감사 보강) · **/change-log**(필터·INSERT 트리거·네비) · **연차**(leaves+산식) · 로그인 문구 |

- DB: Supabase(linked ilukscvucabsxbfmipbt)에 마이그레이션 3건 적용 완료 — `20260826010000`(employment_type) · `020000`(log_insert) · `030000`(leaves)
- 더미 분포: 정규 102 · 계약 37 · 인턴 12 · 별정 6 · 회장 1 / leaves 553건

## 보류 (다음 세션)

| 항목 | 내용 | 비고 |
|---|---|---|
| role 세분화 | 관리자/조회 2종 + RLS role 전환 + 관리 UI (9차 5단계) | 회귀 위험 최대라 미팅 전야 보류 — 계획: `~/.claude/plans/wise-stargazing-bengio.md` |
| Disable signup | Supabase 대시보드 → Auth → Sign up 비활성 **수동 처리** | UI signup은 제거했으나 서버 엔드포인트는 열려 있음. 계정 발급 = `SUPABASE_SERVICE_ROLE_KEY=<키> bash scripts/create_user.sh <이메일> <비번>` |

## 인터뷰(8/26 14:00) 결과 반영 대기

| 항목 | 현 상태 = 가정 | 인터뷰 확정 후 |
|---|---|---|
| 연차 산정 | 근로기준법 기본(입사일 기준 15일+가산·1년 미만 월 1일) — 화면 콜아웃 표기 | 회계연도 기준·이월이면 `src/lib/leave.ts` 산식 교체 |
| 수집 예정 배지 자리 | 스키마 없음 — UI 자리만 (기본·조직·급여·법정·경력 탭) | 확정 항목부터 컬럼 추가 + 배지 해제 |
| 성과 이력 (평가) | 경력·자격 탭 최하단 임시 배치 + 보류 콜아웃 | 평가 탭 신설 여부 결정 |
| 발령유형·고용형태 5종·사번 규칙 | 현행 가정 유지 | 실무 대조 결과 반영 |

## 전제 (뒤집으면 안 되는 확정)

- 디자인 토큰 값 불변 (`design-tokens.css` — DI Works 실측 정본) · 표 셀 13px · 색 = 주황(강조)+상태 점
- 시각 변경은 **반영 후 확대 스크린샷 검증·첨부**가 완료 조건 (260825 확정 규칙 — 문자 글리프 캐럿 오판 재발 방지)
- `supabase db reset` 실행 시 직후 `bash scripts/seed_demo_user.sh` 필수 (auth 계정 소실)
- `supabase db push`는 세션 권한상 차단될 수 있음 — 사용자에게 `! supabase db push` 직접 실행 요청이 확립된 경로
